@model IEnumerable<MedicalManager.Models.ActivityRecord>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Activity.ActivityType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Activity.ActivityType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ActivityRecordId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ActivityRecordId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ActivityRecordId })
        </td>
    </tr>
}

</table>
